.DS_Store
.build/
/Packages
/*.xcodeproj
xcuserdata/
Package.resolved
.swiftpm/
db.sqlite*
.env

# SPA build output (regenerated by `cd web && npm run build`)
Public/index.html
Public/assets/

# SPA local
web/node_modules/
web/dist/
web/.vite/

# Menubar app build outputs
mac/Keywordista.app/
mac/BundledService/
# Generated app icon — derived from mac/Resources/AppIcon.png by
# mac/generate-icon.sh on every build. Only the source PNG is committed.
mac/Resources/AppIcon.icns

# Release artifacts
releases/
*.dmg
*.pkg
*.tar.gz

# Editor / agent scratch dirs
.claude/
.superpowers/
